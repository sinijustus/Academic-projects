plugins {
    id("com.android.application")
    id("kotlin-android")
    id("dev.flutter.flutter-gradle-plugin")
    id("com.google.gms.google-services")
}

android {
    namespace = "com.example.skillaura"
    compileSdk = 35  // ✅ Set explicitly

    ndkVersion = "25.2.9519653"  // ✅ Replace with your NDK version

    compileOptions {
        sourceCompatibility(JavaVersion.VERSION_17)  // ✅ Java 17
        targetCompatibility(JavaVersion.VERSION_17)
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.example.skillaura"
        minSdk = 23
        targetSdk = 34  // ✅ Set explicitly
        versionCode = 1
        versionName = "1.0"
    }

    signingConfigs {
        release {
            storeFile = file("E:/skillaura/keystore.jks")  // ✅ Replace with your keystore path
            storePassword = "your-store-password"
            keyAlias = "your-key-alias"
            keyPassword = "your-key-password"
        }
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.release  // ✅ Correct signing config
            minifyEnabled = true  // ✅ Enable ProGuard for optimization
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation(platform("com.google.firebase:firebase-bom:33.8.0"))
}
